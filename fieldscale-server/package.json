{
  "name": "fieldscale-server",
  "version": "1.0.0",
  "description": "Fieldscale takeoff app — backend (auth, project storage, Claude API proxy)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
